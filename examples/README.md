# Example Output

This directory is reserved for release-ready example artifacts such as sample `REPOPACK.md` and `repo-map.json` files generated against public demo repositories.

It is intentionally ignored by default during repo scans so RepoPack does not include its own examples in generated context packs.